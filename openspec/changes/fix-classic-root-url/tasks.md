## 1. Remove static media patterns from example URL configs

- [ ] 1.1 Remove the `if settings.DEBUG` / `static()` block from `examples/classic/urls.py`
- [ ] 1.2 Remove the `if settings.DEBUG` / `static()` block from `examples/unfold_celery/urls.py`
- [ ] 1.3 Remove the `if settings.DEBUG` / `static()` block from `examples/unfold_rq/urls.py`
