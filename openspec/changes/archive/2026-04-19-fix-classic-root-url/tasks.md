## 1. Remove static media patterns from example URL configs

- [x] 1.1 Remove the `if settings.DEBUG` / `static()` block from `examples/classic/urls.py`
- [x] 1.2 Remove the `if settings.DEBUG` / `static()` block from `examples/unfold_celery/urls.py`
- [x] 1.3 Remove the `if settings.DEBUG` / `static()` block from `examples/unfold_rq/urls.py`
