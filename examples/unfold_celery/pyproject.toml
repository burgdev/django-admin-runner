[project]
name = "example-unfold-celery"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django-admin-runner[celery,unfold]",
    "django-celery-beat>=2.5",
    "django-celery-results>=2.5",
]

[tool.uv.sources]
django-admin-runner = { path = "../..", editable = true }
