[project]
name = "example-classic"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django-admin-runner",
]

[tool.uv.sources]
django-admin-runner = { path = "../..", editable = true }
